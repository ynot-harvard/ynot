import filesystem.buffercache.*;
import filesystem.disk.*;

public class BufferCacheTest {
/*
	public static void main(String[] args) {
		Disk disk = new Disk(4096);
		BufferCache bcache = new BufferCache(disk, 100, 512);
		int numblocks = 32767;
		int nullCount = 0;
		for (int i = 0; i < numblocks; i++) {
			Buffer buf = bcache.getblk(i);			
			if (buf == null)
				throw new RuntimeException();
		}
		
		//System.out.println("done: " + (-10 % 4));
	}*/
}
