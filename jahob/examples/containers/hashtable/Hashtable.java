class Hashtable {

    /*
      Use AssocList.java to implement buckets.
      Use private (static for now) array.

      Introduce a technique for proving that a function is
      deterministic and allow the introduction of skolem functions
      that specify its result, even if we cannot express this
      result in Isabelle.
     */
}
